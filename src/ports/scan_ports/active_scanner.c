#include "ports/scan_ports/active_scanner.h"
#include <stdio.h>

void scan_ports_tcp(int start_port, int end_port) {
    // Aquí irá la lógica de escaneo activo
    printf("Escaneando puertos TCP del %d al %d...\n", start_port, end_port);
}